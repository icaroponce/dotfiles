# Custom (or not) keybinds for my Thinkpad T450s

# Increase volume
"pactl set-sink-volume @DEFAULT_SINK@ +5000"
    XF86AudioRaiseVolume

# Decrease volume
"pactl set-sink-volume @DEFAULT_SINK@ -5000"
    XF86AudioLowerVolume

# Mute volume
"pactl set-sink-mute @DEFAULT_SINK@ toggle"
    XF86AudioMute

# Increase backlight
"xbacklight -inc 10"
   XF86MonBrightnessUp

# Decrease backlight
"xbacklight -dec 10"
   XF86MonBrightnessDown

""
   Shift + space

"scrot ~/Screenshots/$(date '+%Y-%m-%dT%H:%M:%S.png')"
   Print
